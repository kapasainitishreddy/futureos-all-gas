# Public Vite configuration (not a secret)
VITE_CONVEX_URL=

# Set sponsor credentials on the Convex deployment, never in this file:
# npx convex env set OPENAI_API_KEY "..."
# The server is intentionally locked to the low-cost gpt-5.6-luna model.
# npx convex env set FIRECRAWL_API_KEY "..."
# npx convex env set FIRECRAWL_WEBHOOK_SECRET "..."  # optional for one-shot search
# npx convex env set AGENTMAIL_API_KEY "..."
# npx convex env set AGENTMAIL_WEBHOOK_SECRET "..."
